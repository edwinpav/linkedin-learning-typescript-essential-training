{
    "compilerOptions": {
        "outDir": "build",
        "target": "esnext", // emit code compatible with latest JS version
        "noEmit": true, // don't write anything to disk (JS code)
        "moduleResolution": "node"
    },
    "include": ["src/**/*"] // location of which files ts should compile (tsc command)
}